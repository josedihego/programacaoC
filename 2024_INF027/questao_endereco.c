#include <stdio.h>
#include <stdlib.h>


int main(){
    char endereco [200];
    printf("Informe seu endereço completo:");
    fgets(endereco, 200,stdin);
    int i = 0;
    while(endereco[i] != '\0'){
        if(endereco[i])
    }
}