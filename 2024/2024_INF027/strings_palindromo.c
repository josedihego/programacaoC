#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){
    char texto [100];
    char invertido[100];
    printf("Informe um texto para checar se palíndromo: ");
    fgets(texto,100,stdin);
}